# ベースイメージとして公式 ngrok を使用
FROM ngrok/ngrok:latest

# 権限を root に変更
USER root

# 必要なツールをインストール
RUN apt-get update && apt-get install -y \
    curl \
    jq \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# ngrok ユーザーに戻す
USER ngrok